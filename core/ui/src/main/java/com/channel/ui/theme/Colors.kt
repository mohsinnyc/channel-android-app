package com.channel.ui.theme

import androidx.compose.ui.graphics.Color

object Colors {
    val primary = Color(0xFFFFFC00) // Soft Yellow (Snapchat-inspired)
    val secondary = Color(0xFFFFA726) // Warm Orange
    val background = Color(0xFFFFFFFF) // Pure White for a clean look
    val surface = Color(0xFFF8F9FA) // Light Grey for subtle contrast
    val error = Color(0xFFD32F2F) // Softer Red for warnings

    val textPrimary = Color(0xFF212121) // Dark Gray for readability
    val textSecondary = Color(0xFF616161) // Lighter Gray for less emphasis
    val buttonBackground = Color(0xFF333333) // Dark for contrast on white
    val buttonText = Color(0xFFFFFFFF) // White for readability
}
